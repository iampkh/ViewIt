package com.noname.demo;

import com.noname.demo.util.Logger;

public class ErrorHandler {
    public static void sendErrorData(String error){
        //Do implementation to send the error data to server for implementing.
        Logger.eLog(error);
    }
}
